{
  "name": "gemara-explorer",
  "version": "1.0.0",
  "description": "Interactive Gemara study tool with AI-powered explanations",
  "main": "server.js",
  "scripts": {
    "server": "node server.js",
    "client": "cd client && npm start",
    "dev": "npx concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "start": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
